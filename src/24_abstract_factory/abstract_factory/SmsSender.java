package abstract_factory;

public class SmsSender implements Sender {

    @Override
    public void send(String to, String message) {
        // TODO: Geben Sie aus: "SMS an " + to + ": " + message
    }
}
